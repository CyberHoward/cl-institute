# Contract Execution — End Node
#
# Final approval decision where the authorized signer executes the
# contract via DocuSign, the purchase order is recorded in SAP, and
# all documentation is archived for audit. This closes the
# procurement workflow.

id = "aa000004-aaaa-4bbb-8ccc-ddddeeee0004"
workflow_id = "f1000001-aaaa-4bbb-8ccc-ddddeeee0001"
node_type = "end"
label = "Contract Execution"
decision_type = "approval"
index = 3
x = 850.0
y = 300.0
requires_authority = 2
output_schema = """
{
  "type": "object",
  "properties": {
    "contract_id": { "type": "string" },
    "purchase_order_number": { "type": "string" },
    "execution_date": { "type": "string", "format": "date" },
    "contract_term_months": { "type": "integer" },
    "final_amount": { "type": "number" },
    "docusign_envelope_id": { "type": "string" }
  },
  "required": ["contract_id", "purchase_order_number", "execution_date", "final_amount"]
}
"""
created_at = "2025-01-16T09:15:00Z"
updated_at = "2025-01-16T09:15:00Z"
